# 更新日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Changed

- 文档与计划统一迁入 `docs/`：设计说明、历史计划归档、未做功能目录 `docs/todo/` 与「版本↔计划↔完成工作」对照表；根目录不再放置 `PLAN-*.md`

## [0.9.7] - 2026-08-10

### Fixed

- macOS：原生全屏 Space 上翻译窗不显示。改为不可见 `NSPanel` 锚点 + 子窗口挂载（`fullScreenAuxiliary` 仅对 Panel 生效），全屏看视频划词/复制后仍能在右下角看到译文；保留 Dock 图标；Windows 不变

## [0.9.6] - 2026-08-10

### Fixed

- macOS：后台 / 菜单栏常驻时也能感知其他应用的 ⌘C（轮询 `NSPasteboard.changeCount`）；Windows 仍只用 Qt `dataChanged`
- macOS：置顶翻译窗可出现在系统全屏 Space 之上（`canJoinAllSpaces` + `fullScreenAuxiliary`）；Windows 行为不变

## [0.9.5] - 2026-08-09

### Added

- 设置对话框底部固定按钮左侧常显当前版本号与本版发布日期，并保留「检查更新」
- 检查更新结果展示发布日期；非自动覆盖环境改为打开对应正式版的 GitHub 发布页

### Changed

- 正式版检查会读取 GitHub `published_at`（按 Asia/Shanghai 显示日历日）与 `html_url`

### Fixed

- 检查更新时在设置页显示「正在检查…」并禁用按钮；结果弹窗挂在设置窗前，避免被挡住
- 忙碌时再次点击不再弹出易误解的「正在检查或下载更新，请稍候」
- 修复检查与下载 Worker 被提前回收，导致界面永久停留在「正在检查…」的问题
- 修复更新应用脚本的外部 PID 查询可能永久阻塞；便携版改为重试覆盖，成功后才重启新版

## [0.9.3] - 2026-08-09

### Fixed

- 本机桥接忽略浏览器提前断开连接（WinError 10054），避免控制台刷无关 traceback

## [0.9.2] - 2026-08-08

### Fixed

- 开发人员模式「加载解压缩的扩展」时不再打开尚未部署的远程引导页（避免 404）

## [0.9.1] - 2026-08-08

### Changed

- 扩展正式分发改为仅 **Microsoft Edge Add-ons**（不上架 Chrome Web Store）；引导页与「安装浏览器扩展」入口以 Edge 为准

## [0.9.0] - 2026-08-08

### Added

- 打包版首次运行与设置/托盘提供「安装浏览器扩展」引导，打开本机状态探测引导页
- Windows 安装程序可注册 Chrome/Edge Native Messaging，并附带 `ClipboardTranslatorNmHost`；扩展支持零点击自动配对（短码仍作兜底）
- 扩展构建固定公钥以稳定扩展 ID；`onInstalled` 打开引导页；popup 区分自动连接 / 短码 / 离线状态
- 默认启用本机桥接（`[bridge] enabled = true`），便于新用户装完扩展即可连接

### Changed

- 普通用户扩展分发路径改为「商店 Unlisted（上架后）+ 引导页」；开发者模式加载 zip 仅作过渡/开发备用

## [0.8.5] - 2026-08-08

### Fixed

- YouTube 字幕划词改为 pointer capture + 自绘高亮，不再依赖浏览器原生选区，播放中拖选更稳；失败会 toast 提示
- 桌面端对剪贴板/桥接原文折叠所有空白为单空格，避免原文出现换行

## [0.8.4] - 2026-08-08

### Fixed

- 字幕划词原文改为按词索引空格拼接，避免 flex 换行选区把一词拆成多行
- 拖选期间冻结字幕重绘，播放中 cue 刷新不再打断划词；间隙起拖会吸附到最近单词

## [0.8.3] - 2026-08-08

### Fixed

- 字幕拖选后改为经本机桥接直接唤起桌面翻译窗，不再依赖浏览器剪贴板事件（此前常需手动 Ctrl+C）

## [0.8.2] - 2026-08-08

### Added

- YouTube 交互字幕支持拖选一段文本：松手后写入系统剪贴板，由桌面端完成整段翻译；单击单词仍为点词查义

## [0.8.1] - 2026-08-08

### Changed

- YouTube 交互字幕改为相对播放器定位并上移避让控制栏/底部信息区，减少遮挡点赞与分享栏
- 查词弹层支持标题栏拖动与右下角缩放，尺寸在同一标签页内记住；内容局部更新以免丢失交互

## [0.8.0] - 2026-08-08

### Added

- 浏览器本机桥接：桌面端可启用仅监听 `127.0.0.1` 的查词服务，配对后供扩展调用（API Key 不进浏览器）
- YouTube 字幕点词扩展：点击字幕单词即暂停，基于整句语境返回词义，关闭弹层后可安全恢复播放
- 设置中增加「浏览器集成」开关、端口、开始/撤销配对；扩展弹窗可检查在线状态并完成配对
- CI / 本地脚本可构建扩展 zip，随 Windows 预览与正式 Release 一并发布

## [0.7.0] - 2026-08-08

### Added

- 托盘与设置中可「检查更新」：从 GitHub 正式版下载并覆盖安装版/便携版后自动重启
- Windows CI 与本地打包脚本在构建前从 SVG 重新生成 `app.ico`，减少云端包图标过旧

### Fixed

- 说明覆盖安装后任务栏仍显示旧图标时多为 Windows 图标缓存（与托盘运行时图标来源不同）

## [0.6.0] - 2026-08-08

### Added

- 无边框主窗口支持拖动四边与四角缩放，便于按需调整译文阅读区域

## [0.5.0] - 2026-08-08

### Added

- 支持在 macOS 上运行与打包（`.app` + zip）；安装版配置与历史写入 `~/Library/Application Support/ClipboardTranslator`
- CI 增加 macOS 构建，预览版与正式版 Release 可附带 `*-macos.zip`

### Changed

- 打包态用户目录按平台选择（Windows `%APPDATA%`、macOS Application Support、其他 XDG）
- UI 字体随系统选择；无系统托盘时给出提示并保持主窗口可用

## [0.4.0] - 2026-08-08

### Added

- 设置中可直接填写 API URL、API Key 与模型名，保存后立即生效，无需手改配置文件后重启

### Changed

- 应用 exe、桌面快捷方式与系统托盘统一使用剪贴板样式图标

## [0.3.3] - 2026-08-08

### Fixed

- 费用状态行改为占满窗口宽度后再换行，并修正 QLabel 换行宽度计算，避免 hit/费用被过早拆成多行

## [0.3.2] - 2026-08-08

### Fixed

- 单次费用与 cache 节省改为同一行展示，避免被人为拆成两行后半截难读
- 余额刷新改为串行合并请求，避免并发时成功结果被丢掉而误显示「余额暂不可用」

## [0.3.1] - 2026-08-08

### Changed

- 日计费行去掉「约 a:b」比例，只保留今日已用与账户剩余，避免难懂的整数比

## [0.3.0] - 2026-08-08

### Added

- 主窗展示今日已用（本机历史合计）与 DeepSeek 账户剩余余额；启动与每次翻译后异步刷新

## [0.2.1] - 2026-08-08

### Fixed

- 启动首次显示时将窗口锚定到屏幕可用区域右下角；用户拖动后不再强拉；切换置顶时保留原位置
- 剪贴板改为 settle + confirm 两段确认：语音工具临时写入后在确认窗内还原时，不更新原文/译文、不请求模型，避免闪烁

## [0.2.0] - 2026-08-08

### Added

- Windows 便携版与安装版自动打包：push `main` 刷新 Pre-release `preview`，新版本号自动打 `vX.Y.Z` 正式 Release
- 打包后配置与翻译历史写入 `%APPDATA%\ClipboardTranslator`，升级安装不丢数据；首次运行从示例配置落盘
- 安装包支持开始菜单、卸载项与可选开机自启

### Changed

- 运行时路径统一经 `paths.py`：打包资源只读、用户数据可写，源码运行仍使用仓库目录

## [0.1.1] - 2026-08-08

### Fixed

- 缓存命中路径未取消进行中的翻译，语音输入短暂改写剪贴板后再还原时会出现「原文是旧内容、译文却是口述」错位
- 剪贴板变更增加短时防抖：稳定后若内容仍等于上一条则忽略，避免秘塔等语音工具的写入→还原误触发
- 「复制译文」改为按内容/时间窗忽略自身写入，避免 `dataChanged` 迟到导致译文再被当原文翻译
- 剪贴板触发时只显示并置顶窗口、不再抢键盘焦点，降低干扰语音粘贴的概率

## [0.1.0] - 2026-08-08

### Added

- 引入版本号管理：`version.py` 为唯一版本源
- 引入本更新日志 `CHANGELOG.md`
- 约定后续每次功能/修复改动同步升版并记录变更
- 无边框标题栏：置顶、历史、设置、最小化、关闭；可拖拽移动
- 按 DeepSeek usage 估算每次翻译费用与 cache 节省，状态栏展示
- 翻译历史按日 JSONL 永久保留（`data/history-YYYY-MM-DD.jsonl`），历史窗可按日期回看并回填主窗
- 字号设置可写回 `config.toml`

### Notes

- 此版本作为当前功能基线归档（剪切板监听、流式翻译、置顶窗口等）
- 原实施笔记 `PLAN-billing-history.md`（随 Initial commit 于 2026-08-08 16:36 入库，描述费用展示与标题栏/历史方案）内容已并入本条，文件已删除
